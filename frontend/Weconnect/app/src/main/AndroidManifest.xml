<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Weconnect"
        android:usesCleartextTraffic="true">

        <activity
            android:name="com.example.weconnect.activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.weconnect.activities.RegisterActivity"
            android:exported="false" />

        <activity
            android:name="com.example.weconnect.activities.MainActivity"
            android:exported="false"
            android:launchMode="singleTop" />
        <activity
            android:name="com.example.weconnect.activities.CreatePostActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activities.SearchActivity" />
        <activity android:name=".activities.ArchivePostsActivity" />
        <activity android:name=".activities.ChatListActivity" android:launchMode="singleTop" />
        <activity android:name=".activities.ConversationActivity" />
        <activity android:name=".activities.UserProfileActivity" android:launchMode="singleTop" />
        <activity android:name=".activities.PostDetailActivity" />
        <activity android:name=".activities.EditProfileActivity" />
        <activity android:name=".activities.ChangePasswordActivity" />
        <activity android:name=".activities.ParticipantsActivity" />
        <activity android:name=".activities.NotificationsActivity" android:launchMode="singleTop" />
        <activity android:name=".activities.OnboardingActivity" />
        <activity android:name=".activities.ForgotPasswordActivity" />
        <activity android:name=".activities.BlockedUsersActivity" />
        <activity android:name=".activities.LegalActivity" />
        <activity android:name=".activities.PendingListActivity" />

        <service
            android:name=".fcm.WeConnectMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
